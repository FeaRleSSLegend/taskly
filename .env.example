# Postgres connection used by the app and by Alembic migrations.
DATABASE_URL=postgresql+psycopg2://postgres:password@localhost:5432/roadmap

# Secret used to sign JWTs. Generate one with:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=oursecret1234

JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
RESEND_API_KEY=
